import { createContext } from "react";

//First i was handling side bar using context now this code is just junk
const sidebarstatus = createContext({
    visible: true,
})
export default sidebarstatus;